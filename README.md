# m

Educational HTTP proxy server using Node.js.

## Installation

Install dependencies:

```bash
npm install
```

## Usage

Start the proxy server:

```bash
npm start
```

The server will run on `http://127.0.0.1:8080` and proxy requests to `http://example.com`.

## Note

This is for educational purposes only. The proxy binds to localhost only for security.